description: Invalid request payload
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 500
        message:
          type: string
          example: Validation failed phoneNumber, contactType, name is required
